{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // eve builds on Nitro and runs on Node, not Bun.
    "types": ["node"],
    "module": "esnext",
    "esModuleInterop": true
  },
  "include": ["agent/**/*.ts", "evals/**/*.ts"]
}
