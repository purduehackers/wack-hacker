{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Eve builds on Nitro/Node; Bun types cover the package's compiler and tests.
    "types": ["node", "bun"],
    "module": "esnext",
    "esModuleInterop": true
  },
  "include": ["agent/**/*.ts", "evals/**/*.ts", "scripts/**/*.ts"]
}
