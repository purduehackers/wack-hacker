export enum ScheduledTaskStatus {
  Active = "active",
  Cancelled = "cancelled",
  Completed = "completed",
  Failed = "failed",
}

export enum ScheduleType {
  Once = "once",
  Recurring = "recurring",
}
